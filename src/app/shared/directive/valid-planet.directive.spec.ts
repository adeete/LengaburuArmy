import { ValidPlanetDirective } from './valid-planet.directive';

describe('ValidPlanetDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPlanetDirective();
    expect(directive).toBeTruthy();
  });
});
